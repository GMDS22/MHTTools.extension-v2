<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Excel Import / Update" Width="640" Height="520" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Excel Source" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="tb_excel_path" Grid.Row="0" Grid.Column="0" Margin="0,0,6,6"/>
                <Button Content="Browse" Grid.Row="0" Grid.Column="1" Click="browse_click"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,6,0,0" Text="Sheet"/>
                <ComboBox x:Name="cb_sheet" Grid.Row="1" Grid.Column="0" Margin="0,26,0,0" SelectionChanged="sheet_changed"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Output" Grid.Row="1" Margin="0,0,0,10">
            <Grid Margin="8">
                <StackPanel>
                    <RadioButton x:Name="rb_drafting" Content="Drafting View Table" IsChecked="True" Checked="output_changed"/>
                    <RadioButton x:Name="rb_schedule" Content="Schedule View Table" Margin="0,6,0,0" Checked="output_changed"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Header="Target" Grid.Row="2" Margin="0,0,0,10">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Name" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="tb_target_name" Grid.Column="1"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Notes" Grid.Row="3" Margin="0,0,0,10">
            <TextBlock x:Name="tb_notes" Margin="8" TextWrapping="Wrap"/>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Import" Style="{StaticResource PrimaryButton}" Click="import_click"/>
            <Button Content="Update" Style="{StaticResource PrimaryButton}" Click="update_click"/>
            <Button Content="Close" Style="{StaticResource PrimaryButton}" Click="close_click"/>
        </StackPanel>
    </Grid>
</Window>
