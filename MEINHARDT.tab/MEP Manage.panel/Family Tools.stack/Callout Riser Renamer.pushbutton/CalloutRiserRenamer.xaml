<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Callout Riser Renamer" Width="900" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Topmost="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Selected callouts and proposed names" FontWeight="Bold"/>
            <TextBlock Text="  ("/>
            <TextBlock x:Name="CountText" Text="0"/>
            <TextBlock Text=" items)"/>
        </StackPanel>

        <DataGrid x:Name="CalloutGrid" Grid.Row="1" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column" IsReadOnly="False"
                  MinHeight="250" MaxHeight="500">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Rename" Binding="{Binding Selected}" Width="70"/>
                <DataGridTextColumn Header="Current Name" Binding="{Binding CurrentName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="New Name" Binding="{Binding NewName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Reference Level" Binding="{Binding ReferenceLevel}" Width="160" IsReadOnly="True"/>
                <DataGridTextColumn Header="Riser Values" Binding="{Binding RiserValues}" Width="200" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Select All" Width="90" Margin="0,0,6,0" Click="select_all_click"/>
            <Button Content="Clear" Width="70" Margin="0,0,18,0" Click="clear_all_click"/>
            <Button Content="Rename" Width="110" Margin="0,0,6,0" IsDefault="True" Click="apply_click"/>
            <Button Content="Cancel" Width="90" IsCancel="True" Click="close_click"/>
        </StackPanel>
    </Grid>
</Window>
